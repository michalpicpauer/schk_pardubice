# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html
parameters:
    #parameter_name: value

services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    # makes classes in src/AppBundle available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    AppBundle\:
        resource: '../../src/AppBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/AppBundle/{Entity,Repository,Tests}'

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    AppBundle\Controller\:
        resource: '../../src/AppBundle/Controller'
        public: true
        tags: ['controller.service_arguments']

    # add more services, or override services that need manual wiring
    # AppBundle\Service\ExampleService:
    #     arguments:
    #         $someArgument: 'some_value'

    AppBundle\Manager\PostManager:
        class: AppBundle\Manager\PostManager
        arguments:
            $postManager: '@sonata.news.manager.post'

    # Sonata Admin
    admin.page:
        class: AppBundle\Admin\PageAdmin
        arguments: [null, AppBundle\Entity\Page, AppBundle:PageAdmin]
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.cms, label: Pages, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        calls:
            - [ setTranslationDomain, [AppBundle]]
            - [ setTemplate, ['create', 'AppBundle:admin/pages:select_page_type.html.twig']]

    admin.event:
        class: AppBundle\Admin\EventAdmin
        arguments: [null, AppBundle\Entity\Page, null]
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.cms, label: Events, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        calls:
            - [ setTranslationDomain, [AppBundle]]

    admin.page_event:
        class: AppBundle\Admin\EventPageAdmin
        arguments: [null, AppBundle\Entity\PageEvent, null]
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.cms, label: PagesEvents, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        calls:
            - [ setTranslationDomain, [AppBundle]]

    admin.media:
        class: AppBundle\Admin\MediaAdmin
        arguments: [null, AppBundle\Entity\Media,null , '@sonata.media.pool']
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.media, label: Media, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        calls:
            - [ setTranslationDomain, [SonataMediaBundle]]

    admin.gallery:
        class: AppBundle\Admin\GalleryAdmin
        arguments: [null, AppBundle\Entity\Gallery,null , '@sonata.media.pool']
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.media, label: Gallery, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        calls:
            - [ setTranslationDomain, [SonataMediaBundle]]

    admin.user:
        class: AppBundle\Admin\UserAdmin
        arguments: [null, AppBundle\Entity\User,null]
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.user, label: Users, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        calls:
            - [ setTranslationDomain, [SonataUserBundle]]

#    admin.group:
#        class: AppBundle\Admin\GroupAdmin
#        arguments: [null, AppBundle\Entity\Group,null]
#        tags:
#            - { name: sonata.admin, manager_type: orm, group: admin.group.group, label: Groups, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
#        calls:
#            - [ setTranslationDomain, [SonataUserBundle]]

    admin.post:
        class: AppBundle\Admin\PostAdmin
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.blog, label: Posts, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        arguments: [null, AppBundle\Entity\Post,null]
        calls:
            - [ setTranslationDomain, [SonataNewsBundle]]

    admin.comment:
        class: AppBundle\Admin\CommentAdmin
        arguments: [null, AppBundle\Entity\Comment,null]
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.blog, label: Comments, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        calls:
            - [ setTranslationDomain, [SonataNewsBundle]]

    admin.category:
        class: AppBundle\Admin\CategoryAdmin
        arguments: [null, AppBundle\Entity\Category,null, '@sonata.classification.manager.context']
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.classification, label: Categories, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        calls:
            - [ setTranslationDomain, [SonataClassificationBundle]]

    admin.collection:
        class: AppBundle\Admin\CollectionAdmin
        arguments: [null, AppBundle\Entity\Collection,null, '@sonata.classification.manager.context']
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.classification, label: Collections, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        calls:
            - [ setTranslationDomain, [SonataClassificationBundle]]

    admin.tag:
        class: AppBundle\Admin\TagAdmin
        arguments: [null, AppBundle\Entity\Tag,null, '@sonata.classification.manager.context']
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.classification, label: Tags, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        calls:
            - [ setTranslationDomain, [SonataClassificationBundle]]

    admin.context:
        class: AppBundle\Admin\ContextAdmin
        arguments: [null, AppBundle\Entity\Context,null, '@sonata.classification.manager.context']
        tags:
            - { name: sonata.admin, manager_type: orm, group: admin.group.classification, label: Contexts, label_translator_strategy: "sonata.admin.label.strategy.underscore" }
        calls:
            - [ setTranslationDomain, [SonataClassificationBundle]]

